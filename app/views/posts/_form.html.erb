<%= bootstrap_form_for post, url: [post] do |f| %>

    <p>
      <%= f.text_field :title %>
      <%= post.errors[:title] %>
    </p>
    <p>
      <%= f.date_select :publish_date %>
      <%= post.errors[:publish_date] %>
    </p>
    <p>
      <%= f.text_field :resume %>
      <%= post.errors[:resume] %>
    </p>
    <p>
      <%= f.text_area :content %>
      <%= post.errors[:content] %>
    </p>
    <p>
      <%= f.file_field :image %>
      <%= post.errors[:image] %>
    </p>

    <%= f.submit 'OK', :class => 'btn btn-success' %>
<% end %>